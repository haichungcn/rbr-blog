<h1 class="text-info">Listing of Categories</h1>

<ul class="listing">
  <%= will_paginate %>
  <% @categories.each do |category| %>
  <li class="article-title">
    <%= link_to "#{category.name}", category_path(category) %>
  </li>
  <% end %>
  <%= will_paginate %>
</ul>